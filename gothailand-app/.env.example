# ==============================================================================
# GoThailand Environment Configuration Example
# ==============================================================================

# 1. Po Province Backend (พอร์ต 5000: จัดการข้อมูล 77 จังหวัด, Interactive SVG Map)
VITE_API_URL=http://localhost:5000
VITE_PROVINCE_API_URL=http://localhost:5000

# 2. Yok Core Backend (รถเช่า /cars, ที่พัก /accommodations, ไกด์ /guides, การจอง /bookings)
# - ค่าเริ่มต้น: ระบบจะเชื่อมต่อตรงกับ Render Cloud (https://gothailand-api.onrender.com/api)
# - หากต้องการรัน Yok Local ให้เอา # ออกแล้วตั้งเป็น http://localhost:5001
# VITE_YOK_API_URL=http://localhost:5001
